import { ProgressBar as CommonProgressBar } from "../common/slider.slint";

export component ProgressBar inherits CommonProgressBar {
    // All functionality is now inherited from the common ProgressBar component
}
