import { VolumeSlider as CommonVolumeSlider } from "../common/slider.slint";

export component VolumeSlider inherits CommonVolumeSlider {
    // All functionality is now inherited from the common VolumeSlider component
}
