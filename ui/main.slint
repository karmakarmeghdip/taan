import { MusicPlayer } from "player.slint";
import { LoginWindow } from "login.slint";

// Export the MusicPlayer directly as MainWindow since it now inherits from Window
export { MusicPlayer as MainWindow }

// Also export LoginWindow for use in authentication flow
export { LoginWindow }
